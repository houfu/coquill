name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Parse version and codename from tag
        id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          VERSION="${TAG%%-*}"
          CODENAME="${TAG#*-}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "codename=$CODENAME" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Set version env
        run: |
          echo "VERSION=${{ steps.version.outputs.version }}" >> $GITHUB_ENV
          echo "CODENAME=${{ steps.version.outputs.codename }}" >> $GITHUB_ENV
          echo "TAG=${{ steps.version.outputs.tag }}" >> $GITHUB_ENV

      - name: Sync plugin directory
        run: bash scripts/build-plugin.sh "${VERSION}"

      - name: Commit updated plugin/
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add plugin/
          git diff --cached --quiet || git commit -m "chore: update plugin for release v${TAG}"
          git push origin HEAD:main

      - name: Build Cowork ZIP
        run: |
          ZIP_NAME="coquill-v${TAG}.zip"
          while IFS= read -r line || [[ -n "$line" ]]; do
            [ -z "$line" ] && continue
            [ "${line:0:1}" = "#" ] && continue
            FILES="$FILES $line"
          done < release-manifest.txt
          zip -r "$ZIP_NAME" $FILES
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV

      - name: Package plugin ZIP
        run: zip -r "coquill-plugin-v${TAG}.zip" plugin/

      - name: Package examples ZIP
        run: |
          ( cd templates/_examples && zip -r "../../coquill-examples-v${TAG}.zip" . )

      - name: Create latest-named aliases
        run: |
          cp "coquill-plugin-v${TAG}.zip" coquill-plugin-latest.zip
          cp "coquill-examples-v${TAG}.zip" coquill-examples-latest.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "CoQuill v${{ env.VERSION }} ${{ env.CODENAME }}"
          files: |
            ${{ env.ZIP_NAME }}
            coquill-plugin-v${{ env.TAG }}.zip
            coquill-examples-v${{ env.TAG }}.zip
            coquill-plugin-latest.zip
            coquill-examples-latest.zip
          generate_release_notes: true
